services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sidekick-professores
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - .:/app
    working_dir: /app
    restart: unless-stopped
